{
  "name": "dashboard-bootstrap",
  "version": "1.0.0",
  "description": "Bootstrap version of the dashboard",
  "scripts": {
    "start": "parcel index.html --port 1234 --open",
    "build": "parcel build *.html --public-url /ABO-Shoelace/"
  },
  "dependencies": {
    "bootstrap": "^4.5.3",
    "jquery": "^3.5.1",
    "popper.js": "^1.16.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3",
    "svgo": "^3.3.2"
  }
}
